% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/myboot2.R
\name{myboot2}
\alias{myboot2}
\title{Graphical Bootstrap Confidence Interval}
\usage{
myboot2(iter = 10000, x, fun = "mean", alpha = 0.05, cx = 1.5, ...)
}
\arguments{
\item{iter}{Integer. Number of bootstrap samples to generate. Default is 10000.}

\item{x}{Numeric vector. The sample data to resample from.}

\item{fun}{Character or function. The summary statistic to apply to each bootstrap sample (e.g., "mean", "sd"). Default is "mean".}

\item{alpha}{Numeric. Significance level (e.g., 0.05 for a 95% confidence interval). Default is 0.05.}

\item{cx}{Numeric. Size of text annotations in the plot. Default is 1.5.}

\item{...}{Additional arguments passed to the histogram plotting function \code{\link{hist}}.}
}
\value{
A list with the following components:
\describe{
  \item{ci}{Confidence interval (numeric vector of length 2)}
  \item{fun}{The statistic function used}
  \item{x}{The original sample}
}
}
\description{
This function generates a bootstrap confidence interval for a statistic (e.g., mean, median)
and overlays the results on a histogram of bootstrap sample statistics. It also returns the
confidence interval invisibly.
}
\examples{
set.seed(123)
x <- rnorm(20, mean = 5, sd = 2)
myboot2(x = x, fun = "mean")


}
